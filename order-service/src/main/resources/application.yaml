com:
  mario:
    kafka:
      bootstrap-servers:
        - localhost:9091
        - localhost:9092
        - localhost:9093
      security-protocol: "PLAINTEXT"
      application-id: "order"
      consumer-group-id: "transformer"
      trusted-packages: "*"

server:
  port: 8083