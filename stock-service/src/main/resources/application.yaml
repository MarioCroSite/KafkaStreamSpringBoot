com:
  mario:
    kafka:
      stock-orders: "stock-orders"
      market-orders-store: "market-orders-store"
      order-full-topic: "orders-full"

      bootstrap-servers:
        - localhost:9091
        - localhost:9092
        - localhost:9093
      security-protocol: "PLAINTEXT"
      application-id: "stock"
      consumer-group-id: "transformer"
      trusted-packages: "*"

server:
  port: 8085